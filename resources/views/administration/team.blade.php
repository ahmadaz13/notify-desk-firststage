@extends('layouts.app')
@section('content')
<div class="p5-wrap"><header class="p5-header"><div><div class="p5-eyebrow">{{ __('notify.navigation.administration') }}</div><h1 class="p5-title">{{ __('notify.team.title') }}</h1><p class="p5-subtitle">{{ __('notify.team.subtitle') }}</p></div><a href="{{ route('administration.team.create') }}" class="p5-btn p5-btn-primary">{{ __('notify.team.add') }}</a></header>
<div class="p5-card table-responsive"><table class="p5-table"><thead><tr><th>{{ __('notify.team.name') }}</th><th>{{ __('notify.team.email') }}</th><th>{{ __('notify.team.role') }}</th><th>{{ __('notify.team.status') }}</th><th>{{ __('notify.common.actions') }}</th></tr></thead><tbody>
@forelse($teamMembers as $member)<tr><td>{{ $member->name }} @if($member->id===auth()->id())<small>({{ __('notify.team.you') }})</small>@endif</td><td>{{ $member->email }}</td><td>{{ __('notify.team.roles.'.$member->role) }}</td><td>{{ $member->is_active ? __('notify.statuses.active') : __('notify.statuses.inactive') }}</td><td><a class="p5-btn p5-btn-ghost p5-btn-sm" href="{{ route('administration.team.edit',$member) }}">{{ __('notify.actions.edit') }}</a>@if($member->id!==auth()->id() && $member->role!=='founder' && $member->is_active)<form method="POST" action="{{ route('administration.team.deactivate',$member) }}" style="display:inline">@csrf<button class="p5-btn p5-btn-ghost p5-btn-sm" type="submit">{{ __('notify.team.deactivate') }}</button></form>@endif</td></tr>@empty<tr><td colspan="5">{{ __('notify.team.empty') }}</td></tr>@endforelse
</tbody></table></div></div>
@endsection
