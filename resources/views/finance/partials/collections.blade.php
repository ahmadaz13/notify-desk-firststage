<section class="p4-card"><div class="p4-card-head"><div><h2 class="p4-card-title">{{ __('notify.finance.collections_workspace') }}</h2><p class="p4-subtitle">{{ __('notify.finance.collections_desc') }}</p></div><a class="p4-btn p4-btn-primary" href="{{ route('collections.index') }}">{{ __('notify.finance.open_collections') }}</a></div>
<div class="p4-kpis"><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.outstanding_invoices') }}</span><span class="p4-kpi-value">{{ $outstandingInvoices->count() }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.overdue_invoices') }}</span><span class="p4-kpi-value">{{ $overdueInvoices->count() }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.partially_paid') }}</span><span class="p4-kpi-value">{{ $partiallyPaidInvoices->count() }}</span></div></div>
<div class="p4-table-wrap"><table class="p4-table"><thead><tr><th>{{ __('notify.common.client') }}</th><th>{{ __('notify.finance.invoice') }}</th><th>{{ __('notify.finance.due_date') }}</th><th>{{ __('notify.common.actions') }}</th></tr></thead><tbody>@forelse($outstandingInvoices->take(10) as $item)<tr><td>{{ $item['invoice']->client?->business_name }}</td><td dir="ltr">{{ $item['invoice']->invoice_number }}</td><td dir="ltr">{{ $item['invoice']->due_date?->toDateString() }}</td><td><a href="{{ route('clients.show',$item['invoice']->client_id) }}">{{ __('notify.finance.open_client_collections') }}</a></td></tr>@empty<tr><td colspan="4">{{ __('notify.finance.no_outstanding_invoices') }}</td></tr>@endforelse</tbody></table></div></section>
