<div class="p4-callout-warning"><strong>{{ __('notify.finance.boundary_note_title') }}:</strong> {{ __('notify.finance.boundary_note_text') }}</div>
<section class="p4-card"><div class="p4-card-head"><div><h2 class="p4-card-title">{{ __('notify.finance.accounting_core') }}</h2><p class="p4-subtitle">{{ __('notify.finance.accounting_core_desc') }}</p></div><a class="p4-btn p4-btn-soft" href="{{ route('finance.index',['section'=>'reports']+request()->query()) }}">{{ __('notify.finance.view_reports') }}</a></div>
<div class="p4-kpis">
@foreach(['cash_available_minor'=>'cash_available','accounts_receivable_minor'=>'accounts_receivable','overdue_receivables_minor'=>'overdue_receivables','recognized_revenue_this_period_minor'=>'recognized_revenue','operating_expenses_this_period_minor'=>'operating_expenses','management_net_income_this_period_minor'=>'net_income'] as $key=>$label)<div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.'.$label) }}</span><span class="p4-kpi-value">{{ $money($reports['dashboard'][$key]) }}</span></div>@endforeach
</div><div class="p4-card-head" style="margin-top:14px"><a href="{{ route('finance.index',['section'=>'reports']+request()->query()) }}">{{ __('notify.finance.profit_loss') }}</a><a href="{{ route('finance.index',['section'=>'reports']+request()->query()) }}">{{ __('notify.finance.balance_sheet') }}</a><a href="{{ route('finance.index',['section'=>'reports']+request()->query()) }}">{{ __('notify.finance.cash_flow') }}</a></div></section>
<section class="p4-card" style="margin-top:20px"><div class="p4-card-head"><div><h2 class="p4-card-title">{{ __('notify.finance.saas_block_title') }}</h2><p class="p4-subtitle">{{ __('notify.finance.saas_block_subtitle') }}</p></div><a class="p4-btn p4-btn-soft" href="{{ route('saas-metrics.index') }}">{{ __('notify.finance.open_subscription_metrics') }}</a></div>
<div class="p4-kpis"><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.mrr') }}</span><span class="p4-kpi-value">{{ $money($saasReport['ending_mrr_minor']) }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.arr') }}</span><span class="p4-kpi-value">{{ $money($saasReport['ending_arr_minor']) }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.active_subscriptions') }}</span><span class="p4-kpi-value">{{ $saasReport['active_subscriptions'] }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.upcoming_renewals') }}</span><span class="p4-kpi-value">{{ $saasReport['renewal_metrics']['due_30_count'] ?? 0 }}</span></div></div></section>
