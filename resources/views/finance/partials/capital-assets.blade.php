<section class="p4-card"><div class="p4-card-head"><div><h2 class="p4-card-title">{{ __('notify.finance.capital_assets_workspace') }}</h2><p class="p4-subtitle">{{ __('notify.finance.capital_assets_desc') }}</p></div>@if(\App\Support\Features::capitalEnabled())<a class="p4-btn p4-btn-primary" href="{{ route('capital-management.index') }}">{{ __('notify.finance.open_capital_assets') }}</a>@endif</div>
<div class="p4-callout-warning">{{ __('notify.finance.capital_invariants_note') }}</div><div class="p4-kpis"><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.capital_funding') }}</span><span class="p4-kpi-value">{{ $money($capitalTotals['funding_minor']) }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.fixed_assets') }}</span><span class="p4-kpi-value">{{ $money($capitalTotals['company_asset_minor'] + $capitalTotals['personal_asset_minor']) }}</span></div></div></section>
