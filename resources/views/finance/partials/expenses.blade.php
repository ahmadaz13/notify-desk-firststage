<section class="p4-card"><div class="p4-card-head"><div><h2 class="p4-card-title">{{ __('notify.finance.expenses_workspace') }}</h2><p class="p4-subtitle">{{ __('notify.finance.expenses_desc') }}</p></div><a class="p4-btn p4-btn-primary" href="{{ route('operating-expenses.index') }}">{{ __('notify.finance.open_expenses') }}</a></div>
<div class="p4-kpis"><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.company_funded') }}</span><span class="p4-kpi-value">{{ $money($expenseTotals['company_minor']) }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.personal_funded') }}</span><span class="p4-kpi-value">{{ $money($expenseTotals['personal_minor']) }}</span></div><div class="p4-kpi-card"><span class="p4-kpi-label">{{ __('notify.finance.active_expenses') }}</span><span class="p4-kpi-value">{{ $recentExpenses->count() }}</span></div></div></section>
